import 'package:flutter/material.dart';

class MemoryTrackerScreen extends StatelessWidget {
  const MemoryTrackerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('기억 트래커 화면'),
      ),
    );
  }
}
