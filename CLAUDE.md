# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This is a new/empty repository with only a `.claude` directory containing local settings. The repository is not initialized as a git repository.

## Current Configuration

- Local Claude Code settings are configured in `.claude/settings.local.json`
- Current permissions allow `Bash(ls:*)` commands

## Development Setup

This repository appears to be a fresh project workspace. Future development will likely require:

1. Initialize the project structure and choose appropriate technology stack
2. Set up version control with `git init`
3. Configure build tools, linting, and testing frameworks based on chosen technology
4. Update this CLAUDE.md file with specific commands once the project structure is established

## Notes for Future Development

- The repository is currently empty except for Claude Code configuration
- No package managers, build tools, or frameworks have been configured yet
- Consider updating this file with specific development commands once the project structure is established

flutter run -d chrome --web-port=3001